import { ComingSoon } from "@/components/ComingSoon";

export default function LeaderboardPage() {
  return (
    <ComingSoon
      eyebrow="Coming soon"
      title="Leaderboard"
      body="Today, this week, all time — ranked by Street Smart IQ, no real names required. We're wiring up accounts and anti-cheat first so the rankings actually mean something."
    />
  );
}
